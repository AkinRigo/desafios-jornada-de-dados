#Crie uma lista com os números de 1 a 10 e use um loop para imprimir cada número elevado ao quadrado
def numero_lista() -> list[int]:
    return(range(1,11))

#função para elevar ao quadrado

for numero_lista in range[1,11]:
    quadrado = numero_lista ** 2
print(f"{numero_lista}² = `{quadrado}")